[build]
builder = "nixpacks"

[deploy]
# Servidor híbrido - healthcheck GARANTIDO + frontend completo
startCommand = "npm run build && node server/hybrid.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[deploy.envs]
NODE_ENV = "production"