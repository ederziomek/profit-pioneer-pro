[build]
builder = "nixpacks"

[deploy]
# Forçar novo deploy - servidor ultra-básico para healthcheck
startCommand = "node server/ultra-basic.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[deploy.envs]
NODE_ENV = "production"