[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run build && tsx server/simple.ts"
healthcheckPath = "/health/simple"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[deploy.envs]
NODE_ENV = "production"