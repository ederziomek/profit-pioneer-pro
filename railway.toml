[build]
builder = "nixpacks"

[deploy]
# Servidor Express simples - frontend React funcionando desde o início
startCommand = "npm run build && node server/express-simple.js"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[deploy.envs]
NODE_ENV = "production"